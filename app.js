let pronoun = ['the','our'];
let adj = ['great', 'big' ];
let noun = ['jogger','racoon'];

 for(let i = 0; i < pronoun.length; i++){
      for(let k = 0; k < adj.length; k++){
         for(let m = 0; m < noun.length; m++){
            console.log(pronoun[i] + adj[k] + noun[m] + ".com")
         }

    }  
    
 }
